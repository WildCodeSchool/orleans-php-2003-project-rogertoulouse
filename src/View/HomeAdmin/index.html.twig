{% extends 'Admin/index.html.twig' %}

{% block admin %}
    <div class="adminNews container">
        <table>
            <thead>
            <tr>
                <td>Titre</td>
                <td>
                    <a class="btn" href="/HomeAdmin/add/">
                        <i class="fas fa-plus"></i>
                    </a>
                </td>
            </tr>
            </thead>
            <tbody>
            {% for new in news %}
                <tr>
                    <td>{{ new.title }}</td>
                    <td>
                        <form action="/HomeAdmin/edit/{{ new.id }}" method="post">
                            <input type="submit" value="EDITER" class="btn">
                        </form>
                        <form action="/HomeAdmin/delete/{{ new.id }}" method="post">
                            <input name="action" value="delete" hidden>
                            <input name="id" value="{{ new.id }}" hidden>
                            <button class="btn"><i class="fas fa-trash-alt"></i></button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}



