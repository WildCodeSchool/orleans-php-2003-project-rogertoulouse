{% extends 'Admin/index.html.twig' %}
{% block admin %}
<div class="container adminArtworks">
    {% if messages is not empty %}
        {% for message in messages %}
            <div class="message">
                {{ message }}
            </div>
        {% endfor %}
    {% endif %}
    <table>
        <thead>
            <tr>
                <th class="id">ID</th>
                <th>Image</th>
                <th>Titre</th>
                <th>Type</th>
                <th>Année</th>
                <th>Carousel</th>
                <th>Description</th>
                <th class="option end" scope="col">
                    <a class="more btn" href="/ArtworksAdmin/add"><i class="fas fa-plus"></i></a>
                </th>
            </tr>
        </thead>
        <tbody>
        {% for artwork in artworks %}
            <tr>
                <td class="id">{{ artwork.idArtwork }}</td>
                <td><img src="/assets/upload/{{ artwork.image }}" alt="{{ artwork.image }}"></td>
                <td class="text-center">{{ artwork.name }}</td>
                <td>{{ artwork.category }}</td>
                <td>{{ artwork.date|date("Y") }}</td>
                <td>
                    {% if artwork.carousel == 1 %}
                        <div class="carousel"><i class="fas fa-check"></i></div>
                    {% endif %}
                </td>
                <td>
                    <p class="td_description px-2">
                        {{ artwork.description }}
                    </p>
                </td>
                <td>
                    <a class="btn" href="/ArtworksAdmin/update/{{ artwork.idArtwork }}"><i class="fas fa-edit"></i></a>
                    <a class="btn" href="/ArtworksAdmin/preDelete/{{ artwork.idArtwork }}"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}


