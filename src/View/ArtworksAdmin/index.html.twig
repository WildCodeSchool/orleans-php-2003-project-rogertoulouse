{% extends 'Admin/index.html.twig' %}
{% block admin %}
<div class="container adminArtworks">
    {% if messages is not empty %}
        {% for message in messages %}
            <div class="message">
                {{ message }}
            </div>
        {% endfor %}
    {% endif %}
    <a href="/ArtworksAdmin/add"><div class="btn">AJOUTER UNE OEUVRE</div></a>
    <table>
        <thead>
            <tr>
                <th class="id">ID</th>
                <th>Image</th>
                <th>Titre</th>
                <th>Type</th>
                <th>Année</th>
                <th>Carousel</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for artwork in artworks %}
            <tr>
                <td class="id">{{ artwork.idArtwork }}</td>
                <td><img src="/assets/upload/{{ artwork.image }}" alt="{{ artwork.image }}"></td>
                <td class="text-center">{{ artwork.name }}</td>
                <td>{{ artwork.category }}</td>
                <td>{{ artwork.date|date("Y") }}</td>
                <td>
                    {% if artwork.carousel == 1 %}
                        <div class="carousel"><i class="fas fa-check"></i></div>
                    {% endif %}
                </td>
                <td>
                    <p class="td_description px-2">
                        {{ artwork.description }}
                    </p>
                </td>
                <td>
                    <div>
                        <a class="btn" href="/ArtworksAdmin/update/{{ artwork.idArtwork }}">MODIFIER</a>
                        <a class="btn" href="/ArtworksAdmin/preDelete/{{ artwork.idArtwork }}">SUPPRIMER</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}


